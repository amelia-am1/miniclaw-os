{
  "id": "mc-contribute",
  "name": "MiniClaw Contribute",
  "description": "Help contributors build plugins and submit PRs to miniclaw-os using their own MiniClaw agent.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "upstreamRepo": {
        "type": "string",
        "description": "Upstream GitHub repo (owner/name)",
        "default": "augmentedmike/miniclaw-os"
      },
      "forkRemote": {
        "type": "string",
        "description": "Git remote name for your fork",
        "default": "origin"
      },
      "agentName": {
        "type": "string",
        "description": "Agent name for attribution (auto-resolved from IDENTITY.md if not set)"
      },
      "ghUsername": {
        "type": "string",
        "description": "GitHub username for attribution (auto-resolved from gh auth if not set)"
      }
    }
  }
}
